# Mega-Prompt: "Keep the Brain, Rebuild the Spine" — AUSTA Care Platform

> **Status:** READY TO EXECUTE — self-contained prompt for swarm orchestration
> **Generated by:** Parreira (Orquestrador DevOps)
> **Date:** 2026-06-26
> **Target:** Coordenação-Cuidado Enterprise / AUSTA Care Platform
> **Repo:** /Users/familia/code/Coordenacao-Cuidado-Enterprise
>
> **⚠️ DO NOT EXECUTE YET — THIS IS A PROMPT TEMPLATE FOR REVIEW**

---

## 0. Contexto para o Orquestrador

### 0.1 O que é este projeto

Plataforma de coordenação de cuidado (healthcare care-coordination) para o mercado brasileiro de saúde suplementar. WhatsApp-first, gamificação, IA para detecção de riscos, scoring clínico.

### 0.2 Estado atual (da análise completa em PLATFORM-REVIEW.md)

| Dimensão | Nota | Detalhe |
|----------|------|---------|
| Lógica clínica | ⭐⭐⭐⭐ | risk-assessment (1564 linhas), emergency-detection (579 linhas), auditService (805 linhas) — **código bom** |
| Schema Prisma | ⭐⭐⭐⭐⭐ | 18 models, 30 enums — modelagem healthcare excelente |
| Infra/Docker/K8s | ⭐⭐⭐⭐ | Multi-stage Dockerfiles, K8s com HPA, Prometheus 17 alert rules |
| Frontend React | ⭐⭐⭐⭐ | React 18 + Vite 5 + TailwindCSS + PWA — moderno |
| Gamificação | ⭐⭐⭐⭐ | Mission/HealthPoints/Achievement — design inovador |
| **Servidor Express** | ⭐ | **123+ erros TypeScript, NestJS decorators em app errada, não compila** |
| **Segurança real** | ⭐ | **pgcrypto nunca usado, audit em memória, secrets hardcoded** |
| **Docs arquitetura** | ⭐ | **Descreve Python/Java que não existem, Meta WhatsApp que é Z-API** |

### 0.3 Estratégia

**"Keep the Brain, Rebuild the Spine"** — Preservar o domínio clínico (risk, emergency, audit, schema, gamification, infra) e reescrever a camada de servidor Express + segurança + documentação.

### 0.4 Modo de Operação

- **Autônomo completo** — zero prompts de confirmação
- **Máximo paralelismo** — 3 agentes por wave, sem dependências intra-wave
- **Stops ONLY:** (1) credenciais faltantes, (2) gastar dinheiro real
- **Commit após cada wave** — `git add -A && git commit -m "wave-N: descrição"`
- **Artefatos regulatórios** → DRAFT → registrar em `docs/review-queue.md`
- **Bloqueios reais** → `HANDOFF.yaml`
- **Spawn Tree** — despachar `sleep 15` agent ao final para demonstrar UI

### 0.5 Arquivos de Referência (LEIA antes de começar)

| Arquivo | Propósito |
|---------|-----------|
| `docs/_analysis/PLATFORM-REVIEW.md` | Relatório completo da análise (728 linhas) |
| `docs/_analysis/adrs-draft.md` | 6 ADRs propostos |
| `docs/_analysis/risk-register-roadmap.md` | Risk register + NOW/NEXT/LATER |
| `docs/_analysis/appendix-glossary-stack.md` | Glossário, acrônimos, stack comparison |
| `docs/_analysis/section-05-architecture.md` | Análise de arquitetura + 12 contradições |
| `docs/_analysis/section-codebase-infra.md` | Qualidade de código + infra |
| `docs/_analysis/section-06-compliance.md` | Compliance + segurança |
| `HANDOFF.yaml` | 7 bloqueios críticos |
| `docs/review-queue.md` | 10 itens para revisão humana |

---

## 1. Plano de Execução — 4 Waves

### 📋 Resumo das Waves

```
Wave 0: FOUNDATION FIXES (3 agentes) → commit
Wave 1: REBUILD THE SPINE (3 agentes) → commit
Wave 2: PLUG THE BRAIN BACK IN (3 agentes) → commit
Wave 3: PRODUCTION HARDENING (3 agentes) → commit
Wave 4: DOCUMENTATION & ADRs (2-3 agentes) → commit
DEMO: Spawn Tree demonstration agent → mostra UI ao vivo
```

---

## 2. Wave 0 — FOUNDATION FIXES

**Objetivo:** Corrigir os 3 problemas mais graves e independentes que não dependem de reescrita de código.

**3 agentes, 100% paralelo, ZERO dependências entre si.**

### Agente 0-A: Fix AWS Region + Build Terraform Modules

**Especialista:** `ops-cloud-provisioning` ou `ops-iac-terraform`
**Toolsets:** terminal, file, web, skills

**Context:** O Terraform está incompleto — apenas `austa-care-platform/infrastructure/terraform/environments/production/terraform.tfvars.example` existe. A região configurada é `us-east-1` (EUA) — deve ser `sa-east-1` (São Paulo, Brasil) por compliance LGPD Art. 33.

**Goal:** Construir módulos Terraform completos para provisionar a infraestrutura AWS em `sa-east-1`.

**Tasks:**
1. Corrigir `aws_region` de `us-east-1` para `sa-east-1` em `terraform.tfvars.example`
2. Criar estrutura completa de módulos Terraform:
   ```
   infrastructure/terraform/
   ├── main.tf                    # Provider AWS sa-east-1, remote state S3+DynamoDB
   ├── variables.tf               # Todas variáveis (region, env, instance sizes)
   ├── outputs.tf                 # Outputs (RDS endpoint, EKS cluster name, etc.)
   ├── terraform.tfvars.example   # Já existe — atualizar
   ├── modules/
   │   ├── vpc/                   # VPC com subnets públicas/privadas, NAT Gateway
   │   ├── eks/                   # EKS cluster + node groups
   │   ├── rds/                   # RDS PostgreSQL 15 com encryption enabled
   │   ├── elasticache/           # Redis cluster
   │   ├── s3/                    # S3 buckets (data lake, backups, artifacts)
   │   └── iam/                   # IAM roles para EKS, RDS, secrets
   └── environments/
       ├── production/
       │   └── main.tf            # Instancia módulos com valores de produção
       └── staging/
           └── main.tf            # Instancia módulos com valores de staging
   ```
3. Configurar remote state: S3 bucket + DynamoDB lock table
4. Aplicar tags obrigatórias: `Environment`, `Project=AustaCare`, `Compliance=LGPD`, `DataClassification=PHI`
5. RDS: `storage_encrypted = true`, `deletion_protection = true`, backup 30 dias
6. Não provisionar recursos reais (não executar `terraform apply`) — apenas escrever o código

**Verification:**
- `terraform fmt -recursive` passa sem alterações
- `terraform validate` passa em cada environment
- Região `sa-east-1` confirmada em todos os providers
- Pelo menos 6 módulos criados

---

### Agente 0-B: Replace HIPAA→LGPD Throughout Codebase

**Especialista:** `ops-compliance-gate`
**Toolsets:** terminal, file, search, skills

**Context:** HIPAA é referenciado 18 vezes no código (schema, docs, package.json, migrations, READMEs, env configs). LGPD aparece apenas 4 vezes. HIPAA é regulação DOS EUA e **não se aplica** a uma plataforma 100% brasileira. Isso é um claim enganoso com risco legal.

**Goal:** Substituir TODAS as referências a HIPAA por LGPD/ANS/ANVISA em todo o repositório. Atualizar o schema Prisma, migrations, documentação, e package.json.

**Tasks:**
1. **Schema Prisma** (`prisma/schema.prisma`):
   - Renomear `hipaaCompliant` → `lgpdCompliant` no model Organization
   - Renomear `hipaaRelevant` → `lgpdRelevant` no model AuditLog
   - Atualizar comentários/documentação inline

2. **Migration SQL** (`prisma/migrations/001_init_austa_care_schema.sql`):
   - Atualizar comentários que mencionam HIPAA
   - Adicionar comentários sobre LGPD/ANS/ANVISA compliance

3. **Documentação**:
   - `prisma/DATABASE_SCHEMA_DOCUMENTATION.md`: Substituir seção "HIPAA Compliance" por "LGPD Compliance"
   - `docs/Requisitos.md`: Atualizar §4.2 para framework LGPD/ANS (não HIPAA)
   - `docs/architecture_diagrams.md`: Atualizar referências de compliance
   - `backend/tests/README.md`: Remover "HIPAA-compliant"

4. **Package.json** (root):
   - Alterar `"description"` de "HIPAA-compliant healthcare platform..." para "LGPD-compliant healthcare coordination platform for Brazilian health plans"
   - Atualizar `"keywords"`: remover "hipaa", adicionar "lgpd", "ans", "anvisa", "saude-suplementar"

5. **Código backend**:
   - Atualizar comentários e strings que mencionam HIPAA
   - Adicionar referências à LGPD onde relevante (especialmente em auditService.ts)

6. **Gerar nova migration Prisma** para renomear colunas: `hipaa_compliant` → `lgpd_compliant`, `hipaa_relevant` → `lgpd_relevant`

**CRITICAL:** Não quebrar a funcionalidade existente. As colunas no banco serão renomeadas mas a lógica permanece igual — apenas os nomes refletem o framework correto.

**Verification:**
- `grep -r "HIPAA\|hipaa" --include="*.ts" --include="*.md" --include="*.json" --include="*.sql" --include="*.prisma" . | grep -v node_modules | grep -v ".git" | grep -v "_analysis"` → ZERO resultados
- `grep -r "LGPD\|lgpd" --include="*.ts" --include="*.md" --include="*.json" . | grep -v node_modules | grep -v ".git"` → >20 resultados
- Migration gerada e validada

---

### Agente 0-C: Secrets Management + Environment Configuration

**Especialista:** `ops-secrets-management`
**Toolsets:** terminal, file, search, skills

**Context:** O projeto tem secrets hardcoded em docker-compose (`austa_password`, `admin123`, `minioadmin`), secrets em `.env` versionados, e o SDK da AWS Secrets Manager está nas dependências mas não é usado. Não existe `.env.example` seguro.

**Goal:** Criar sistema de gestão de secrets adequado para desenvolvimento local e documentar o caminho para produção com AWS Secrets Manager.

**Tasks:**
1. Criar `.env.example` limpo (sem secrets reais) com todas as variáveis necessárias e valores placeholder
2. Atualizar `docker-compose.yml` para usar `${VARIABLE}` ao invés de strings hardcoded:
   ```yaml
   POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_in_production}
   ```
3. Criar script `scripts/generate-dev-secrets.sh` que gera secrets aleatórios para desenvolvimento local
4. Atualizar `docker-compose.infrastructure.yml` com o mesmo padrão
5. Remover secrets hardcoded de TODOS os arquivos (docker-compose, kong.yaml, nginx configs)
6. Criar `docs/SECRETS-MANAGEMENT.md` documentando:
   - Desenvolvimento local: `.env` + `.env.example` (gerado por script)
   - Staging: AWS Secrets Manager (`@aws-sdk/client-secrets-manager` já está nas deps)
   - Produção: AWS Secrets Manager com rotação automática
7. Adicionar `.env` ao `.gitignore` (se ainda não estiver)
8. Verificar se `backend/src/config/` referencia secrets corretamente via `process.env`

**Verification:**
- `grep -r "password.*:.*[a-z].*[0-9]" docker-compose*.yml` → ZERO hardcoded passwords
- `.env.example` existe e tem todas as variáveis com placeholders seguros
- Script de geração funciona: `bash scripts/generate-dev-secrets.sh` produz `.env` funcional

---

## 3. Wave 1 — REBUILD THE SPINE

**Objetivo:** Reescrever a camada de servidor Express com TypeScript estrito, zero erros, sem NestJS, com path aliases funcionando. Test infrastructure unificada.

**Dependência:** Wave 0 completa.

**3 agentes — podem ser paralelos pois trabalham em arquivos diferentes.**

### Agente 1-A: Clean Express Server Foundation

**Especialista:** Leaf agent (TypeScript/Node.js)
**Toolsets:** terminal, file

**Context:** O servidor atual (`backend/src/server.ts`) e `app.ts` têm 123+ erros de compilação, incluindo decorators do NestJS que não pertencem a uma app Express. É mais eficiente reescrever a camada de servidor (não a lógica de domínio) do zero.

**Goal:** Criar um servidor Express limpo, com TypeScript strict, zero erros de compilação, bem estruturado, que servirá como nova espinha dorsal.

**Tasks:**
1. Criar estrutura de diretório limpa:
   ```
   backend/src/
   ├── server.ts              # Entry point — listen, graceful shutdown
   ├── app.ts                 # Express app factory — middleware, routes
   ├── config/
   │   ├── index.ts           # Config loader (dotenv + validação)
   │   └── database.ts        # Prisma client singleton
   ├── middleware/
   │   ├── error-handler.ts   # Global error handler
   │   ├── request-logger.ts  # HTTP request logging
   │   ├── rate-limiter.ts    # Rate limiting
   │   └── auth.ts            # JWT authentication middleware
   ├── routes/
   │   ├── index.ts           # Route aggregator
   │   ├── health.ts          # GET /health
   │   └── auth.routes.ts     # POST /auth/login, /auth/register, /auth/refresh
   ├── lib/
   │   ├── errors.ts          # AppError, NotFoundError, UnauthorizedError classes
   │   └── response.ts        # Standardized API response helpers
   └── types/
       └── express.d.ts       # Express type extensions
   ```

2. `server.ts` — limpo, sem NestJS, sem EventEmitter indevido:
   ```typescript
   import { app } from './app';
   import { logger } from './utils/logger';
   
   const PORT = process.env.PORT || 3000;
   
   const server = app.listen(PORT, () => {
     logger.info(`AUSTA Care Platform API running on port ${PORT}`);
   });
   
   // Graceful shutdown
   const shutdown = async (signal: string) => {
     logger.info(`${signal} received — shutting down gracefully`);
     server.close(() => process.exit(0));
     setTimeout(() => process.exit(1), 10000); // force after 10s
   };
   
   process.on('SIGTERM', () => shutdown('SIGTERM'));
   process.on('SIGINT', () => shutdown('SIGINT'));
   ```

3. `app.ts` — middleware stack correto:
   ```typescript
   import express from 'express';
   import helmet from 'helmet';
   import cors from 'cors';
   import compression from 'compression';
   import { requestLogger } from './middleware/request-logger';
   import { errorHandler } from './middleware/error-handler';
   import { routes } from './routes';
   
   export const app = express();
   
   // Security
   app.use(helmet());
   app.use(cors());
   
   // Parsing
   app.use(express.json());
   app.use(compression());
   
   // Observability
   app.use(requestLogger);
   
   // Routes
   app.use('/api/v1', routes);
   
   // Error handling (MUST be last)
   app.use(errorHandler);
   ```

4. `tsconfig.json` — strict mode:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "commonjs",
       "lib": ["ES2022"],
       "outDir": "./dist",
       "rootDir": "./src",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "forceConsistentCasingInFileNames": true,
       "resolveJsonModule": true,
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       }
     },
     "include": ["src/**/*.ts"],
     "exclude": ["node_modules", "dist", "tests"]
   }
   ```

5. Middleware de erro padronizado com classes de erro tipadas
6. Health check endpoint com verificação de DB + Redis (graceful degradation se Redis indisponível — padrão já implementado no fix-swarm)
7. Rate limiter usando `express-rate-limit`

**CRITICAL:**
- NÃO usar decorators do NestJS
- NÃO usar `EventEmitter` a menos que explicitamente necessário
- NÃO copiar código de outros projetos
- Usar imports com path aliases consistentes (`@/config`, `@/middleware`, `@/routes`)
- Todo arquivo deve compilar com `tsc --noEmit` sem erros

**Verification:**
- `cd backend && npx tsc --noEmit` → ZERO erros
- `curl http://localhost:3000/api/v1/health` → 200 OK
- Sem imports de `@nestjs/*`

---

### Agente 1-B: Unify Test Infrastructure + Fix Prisma

**Especialista:** Leaf agent (TypeScript/Testing)
**Toolsets:** terminal, file, search

**Context:** O projeto tem infraestrutura de teste duplicada (dois sistemas de helpers separados), 16 arquivos de teste para 161 fontes, Prisma version mismatch (root ^6.19.0 vs backend ^5.7.0), e os testes existentes provavelmente não compilam com o server limpo.

**Goal:** Unificar a infraestrutura de teste, resolver o version mismatch do Prisma, e garantir que os testes existentes funcionem com o novo server.

**Tasks:**
1. **Resolver Prisma version mismatch**:
   - Alinhar versão no `backend/package.json` para `^6.19.0` (mesma do root)
   - Regenerar Prisma client: `npx prisma generate`
   - Verificar compatibilidade da migration existente

2. **Unificar test infrastructure**:
   - Consolidar `backend/tests/utils/test-helpers.ts` e `backend/tests/helpers/` em UMA localização
   - Manter `backend/tests/setup.ts` como entry point global
   - Remover `backend/src/tests/` (testes não devem estar em src/)
   - Estrutura final:
     ```
     backend/tests/
     ├── setup.ts                 # Global setup (mock DB, env)
     ├── helpers/
     │   ├── test-factories.ts    # TestDataGenerator + faker factories
     │   └── test-database.ts     # Database test helpers
     ├── unit/
     │   ├── controllers/         # health.test.ts, auth.test.ts
     │   └── services/            # Serviços clínicos (a migrar na Wave 2)
     ├── integration/
     └── e2e/
     ```

3. **Criar jest.config.ts limpo** com path aliases, cobertura, setupFiles

4. **Verificar/atualizar scripts npm de teste**:
   ```json
   "test": "jest --passWithNoTests",
   "test:coverage": "jest --coverage --passWithNoTests",
   "test:watch": "jest --watch",
   "test:ci": "jest --coverage --watchAll=false --passWithNoTests"
   ```

**Verification:**
- `npx prisma generate` sucede sem erros
- `npm test` roda (pode ter 0 testes initially, mas não quebra)
- Estrutura de diretórios limpa sem duplicação

---

### Agente 1-C: Fix All TypeScript Errors + Path Aliases

**Especialista:** Leaf agent (TypeScript)
**Toolsets:** terminal, file, search

**Context:** O codebase tem 123+ erros de compilação. Os principais: `@types/node` ausente (45 erros `process`/`Buffer`), path aliases `@/` quebrados (não resolvem), ~50 erros de `EventEmitter` em serviços que não estendem EventEmitter, NestJS decorators em app Express. O Agente 1-A criou um server limpo; este agente foca nos serviços existentes que serão preservados.

**Goal:** Corrigir TODOS os erros TypeScript nos serviços de domínio que serão preservados (risk, emergency, audit, gamification, WhatsApp, OCR, engagement). O server já está limpo pelo Agent 1-A.

**Tasks:**
1. Instalar `@types/node` no backend/package.json
2. Configurar `tsconfig.json` com `paths` funcionais e `tsconfig-paths` para runtime:
   ```bash
   npm install --save-dev tsconfig-paths
   ```
   Registrar em `package.json`:
   ```json
   "dev": "tsx -r tsconfig-paths/register src/server.ts"
   ```

3. Para CADA serviço com erro `EventEmitter`:
   - Se o serviço realmente precisa emitir eventos → adicionar `extends EventEmitter`
   - Se não precisa → remover chamadas a `this.emit()`, usar logger ou callbacks
   - Prioridade: serviços clínicos (risk, emergency, audit) → corrigir corretamente

4. Para CADA arquivo com import `@/` quebrado:
   - Corrigir para caminho relativo ou configurar alias corretamente
   - Padronizar: usar caminhos relativos para imports dentro do mesmo módulo

5. Remover TODOS os imports de `@nestjs/*` — substituir por equivalentes Express ou remover

6. Corrigir tipos Any → tipos específicos onde possível

7. Não alterar a LÓGICA dos serviços clínicos — apenas corrigir tipos e imports

**CRITICAL:** 
- NÃO quebrar a lógica de domínio clínico
- Priorizar correções mecânicas (tipos, imports) sobre refatoração
- Serviços que não compilam após correções → documentar em `HANDOFF.yaml` com o erro específico

**Verification:**
- `cd backend && npx tsc --noEmit` → IDEAL: 0 erros; ACEITÁVEL: <10 erros (todos documentados)
- Serviços clínicos (risk, emergency, audit) → ZERO erros
- Zero imports de `@nestjs/*`

---

## 4. Wave 2 — PLUG THE BRAIN BACK IN

**Objetivo:** Migrar os serviços de domínio clínico preservados para funcionar com o novo server limpo. Conectar o schema Prisma à lógica de negócio real. Implementar persistência real do audit trail.

**Dependência:** Wave 1 completa (server limpo compilando).

**3 agentes — paralelos pois trabalham em serviços diferentes.**

### Agente 2-A: Migrate Clinical Risk + Emergency Services

**Especialista:** Leaf agent (TypeScript/Healthcare)
**Toolsets:** terminal, file, search, skills

**Context:** Os serviços `risk-assessment.service.ts` (1564 linhas) e `emergency-detection.service.ts` (579 linhas) contêm lógica clínica de alta qualidade que deve ser preservada. Eles precisam ser adaptados para: compilar com o novo server, usar o Prisma para persistência, e registrar versão de algoritmo.

**Goal:** Migrar os serviços de risco e emergência para o novo server, garantindo compilação limpa e integração com o schema Prisma.

**Tasks:**
1. **risk-assessment.service.ts**:
   - Garantir que compila com `tsc --noEmit`
   - Conectar ao Prisma: salvar `AdvancedRiskAssessment` results em `HealthData` model
   - Adicionar `algorithm_version` field nos resultados salvos (ex: `"risk-v1.0-{git-sha}"`)
   - Adicionar `calculated_at` timestamp
   - Registrar no AuditLog via auditService (se disponível)

2. **emergency-detection.service.ts**:
   - Garantir que compila
   - Conectar ao Prisma: persistir `EmergencyAlert` como entidade (pode usar HealthData ou nova tabela)
   - Adicionar `escalation_status` tracking (PENDING → NOTIFIED → ACKNOWLEDGED → RESOLVED)
   - Integrar com notificationService para disparar alertas reais

3. **Criar `backend/src/services/index.ts`** — barrel export para todos os serviços

4. **Criar testes unitários básicos** para cada serviço:
   - Test: cardiovascular risk detection com inputs conhecidos → output esperado
   - Test: emergency detection para tríade de diabetes → flag DIABETES_RISK
   - Test: emergency detection para sinais cardíacos → flag CARDIAC_RISK

**Verification:**
- Ambos serviços compilam com zero erros TypeScript
- `npm test -- --testPathPattern="risk|emergency"` → pelo menos 2 testes passam
- Resultados de risco são persistidos no banco (verificar via Prisma Studio ou query)

---

### Agente 2-B: Migrate Audit Service + Implement pgcrypto Encryption

**Especialista:** `ops-secrets-management` + leaf agent
**Toolsets:** terminal, file, search, skills

**Context:** O `auditService.ts` (805 linhas) tem regras de compliance LGPD/ANS excelentes, mas usa buffer em memória (Map) e mock data — nunca persistiu no banco. O `AuditLog` model do Prisma (polimórfico, 25+ campos) está pronto mas não é usado. O `pgcrypto` PostgreSQL extension está carregada na migration mas ZERO usos no código backend.

**Goal:** Conectar o AuditService ao AuditLog model do Prisma para persistência real. Implementar column-level encryption usando pgcrypto para PHI/PII.

**Tasks:**
1. **AuditService → Prisma AuditLog**:
   - Substituir `auditBuffer: Map` por `prisma.auditLog.create()`
   - Remover mock data e `getCurrentIpAddress()` fake
   - Implementar `storeAuditEntry()` → persistência real no banco
   - Manter buffer para batch flush (performance) mas com persistência garantida
   - Garantir que `recordEvent`, `recordStateTransition`, `recordDataAccess`, `recordSecurityEvent` salvam no banco

2. **Implementar pgcrypto encryption**:
   - Criar `backend/src/lib/crypto.ts`:
     ```typescript
     import { prisma } from '../config/database';
     
     // Envelope encryption: chave por tenant
     export async function encryptPHI(organizationId: string, plaintext: string): Promise<string> {
       const result = await prisma.$queryRawUnsafe<{ encrypted: string }[]>(
         `SELECT encode(pgp_sym_encrypt($1, $2), 'base64') as encrypted`,
         plaintext, await getTenantKey(organizationId)
       );
       return result[0].encrypted;
     }
     
     export async function decryptPHI(organizationId: string, ciphertext: string): Promise<string> {
       const result = await prisma.$queryRawUnsafe<{ decrypted: string }[]>(
         `SELECT pgp_sym_decrypt(decode($1, 'base64'), $2) as decrypted`,
         ciphertext, await getTenantKey(organizationId)
       );
       return result[0].decrypted;
     }
     ```
   - Criptografar campos sensíveis: `User.cpf`, `User.emergencyContact`, `HealthData.conditions` (JSONB), `Document` metadata
   - Adicionar `encrypted_at` timestamp nos registros
   - Implementar key rotation placeholder (comentário + TODO para Vault/KMS)

3. **Criar migration SQL** para habilitar pgcrypto (se não existir) e criar função de envelope encryption

**Verification:**
- `auditService.recordEvent({...})` → registro aparece na tabela `audit_logs`
- `encryptPHI('org-123', 'dado sensivel')` → retorna string base64 != plaintext
- `decryptPHI('org-123', encrypted)` → retorna plaintext original
- AuditLog sobrevive a restart do servidor (persistido no PostgreSQL)

---

### Agente 2-C: Migrate WhatsApp Integration + Gamification Services

**Especialista:** Leaf agent (TypeScript/Integration)
**Toolsets:** terminal, file, search

**Context:** O WhatsApp service atual usa **Z-API** (z-api.io), não a Meta Business API como documentado. O sistema de gamificação (Mission, HealthPoints, Achievement) tem models Prisma excelentes mas a lógica de serviço precisa ser conectada ao novo server.

**Goal:** Documentar honestamente a integração Z-API, migrar os serviços de WhatsApp e gamificação para o novo server, e conectar ao schema Prisma.

**Tasks:**
1. **WhatsApp Integration**:
   - Atualizar `whatsapp.service.ts` para compilar com novo server
   - Documentar que o provider é Z-API (não Meta) — atualizar comentários e docs
   - Adicionar `WHATSAPP_PROVIDER=z-api` como env var explícita
   - Implementar idempotência: `INSERT ON CONFLICT (whatsappMessageId) DO NOTHING`
   - Adicionar retry com backoff para falhas de envio (usando BullMQ ou retry manual)
   - Criar `docs/WHATSAPP-INTEGRATION.md` documentando setup Z-API

2. **Gamification Services**:
   - `missionService.ts` — conectar ao Prisma Mission/OnboardingProgress models
   - `adaptiveMissionEngine.ts` — compilar e integrar
   - Garantir que HealthPoints são calculados e persistidos via PointTransaction

3. **Notification Service**:
   - `notificationService.ts` — multi-canal (WhatsApp, SMS, push)
   - Implementar retry com backoff para falhas
   - Template de mensagem padrão para cada canal

**Verification:**
- WhatsApp webhook endpoint funcional (mock no teste)
- Gamification: criar missão → completar → HealthPoints incrementam
- Documentação Z-API criada e honesta

---

## 5. Wave 3 — PRODUCTION HARDENING

**Objetivo:** Implementar healthcare invariants restantes, CI/CD com deploy, e infraestrutura completa.

**Dependência:** Wave 2 completa.

**3 agentes paralelos.**

### Agente 3-A: Implement Healthcare Invariants

**Especialista:** Leaf agent (TypeScript/Security)
**Toolsets:** terminal, file, search, skills

**Context:** Dos 6 invariantes de healthcare MVP, apenas 1 está parcialmente implementado (audit trail — agora completo após Wave 2). Os outros 5 precisam ser implementados antes do primeiro paciente real.

**Goal:** Implementar os invariantes restantes e verificar todos os 6.

**Tasks:**
1. **✅ Audit trail imutável** — verificado na Wave 2. Validar que AuditLog não tem UPDATE/DELETE permissions na aplicação.

2. **Idempotência de mensagens (INV-2)**:
   - WhatsApp: `INSERT ON CONFLICT (whatsappMessageId) DO NOTHING` — verificado no Agent 2-C
   - FHIR: Adicionar idempotency key nos handlers FHIR
   - Tasy: Adicionar `ON CONFLICT (tasyReferenceId)` nos sync logs

3. **Versionamento de algoritmos (INV-3)**:
   - Adicionar campo `algorithm_version: string` em: HealthData, VitalSign, QuestionnaireResponse, Achievement
   - Criar `backend/src/lib/algorithm-registry.ts`:
     ```typescript
     export const ALGORITHM_VERSIONS = {
       'risk-assessment': '1.0.0',
       'emergency-detection': '1.0.0',
       'symptom-analysis': '1.0.0',
       'population-stratification': '1.0.0',
     } as const;
     ```
   - Todo score clínico salvo deve registrar `algorithm_version`

4. **Encryption at rest (INV-4)** — verificado na Wave 2. Validar em health check.

5. **Health check + dead man's switch (INV-5)**:
   - Endpoint `/api/v1/health` já existe — expandir:
     - Verificar DB connectivity (`SELECT 1`)
     - Verificar Redis (com graceful degradation)
     - Verificar encryption status (`pgcrypto` functional)
   - Adicionar endpoint `/api/v1/health/dead-mans-switch` que retorna timestamp da última atividade
   - Documentar necessidade de monitor externo (Prometheus Blackbox Exporter ou similar)

6. **Retry com backoff (INV-6)**:
   - Unificar retry logic em `backend/src/lib/retry.ts`:
     ```typescript
     export async function withRetry<T>(
       fn: () => Promise<T>,
       options: { maxAttempts?: number; baseDelay?: number } = {}
     ): Promise<T> {
       const { maxAttempts = 3, baseDelay = 1000 } = options;
       for (let attempt = 1; attempt <= maxAttempts; attempt++) {
         try { return await fn(); } catch (error) {
           if (attempt === maxAttempts) throw error;
           await new Promise(r => setTimeout(r, baseDelay * Math.pow(2, attempt - 1)));
         }
       }
       throw new Error('unreachable');
     }
     ```
   - Aplicar a: WhatsApp sends, Tasy API calls, email/SMS notifications

**Verification:**
- Todos os 6 invariantes passam na verificação (planilha em `docs/HEALTHCARE-INVARIANTS.md`)
- Health check endpoint retorna status de todos os componentes

---

### Agente 3-B: Complete CI/CD Pipeline + GitHub Actions

**Especialista:** `ops-cicd-github`
**Toolsets:** terminal, file, search, skills

**Context:** Existem 2 workflows de CI para o backend e CodeQL — mas sem CD/deploy, sem CI para frontend, sem integração com Docker registry.

**Goal:** Criar pipeline CI/CD completa: CI para backend e frontend, build Docker images, push para registry, deploy manifests.

**Tasks:**
1. **CI — Backend** (atualizar workflow existente):
   ```yaml
   name: Backend CI
   on: [push, pull_request]
   jobs:
     type-check:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v4
         - run: cd backend && npm ci
         - run: cd backend && npx tsc --noEmit
     lint:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v4
         - run: cd backend && npm ci
         - run: cd backend && npm run lint
     test:
       needs: [type-check]
       runs-on: ubuntu-latest
       services:
         postgres:
           image: postgres:15-alpine
           env: { POSTGRES_USER: test, POSTGRES_PASSWORD: test, POSTGRES_DB: austa_test }
         redis:
           image: redis:7-alpine
       steps:
         - run: cd backend && npm ci && npx prisma generate && npx prisma db push
         - run: cd backend && npm test -- --coverage
   ```

2. **CI — Frontend** (criar workflow novo):
   ```yaml
   name: Frontend CI
   on: [push, pull_request]
   jobs:
     lint-test:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - run: cd frontend && npm ci
         - run: cd frontend && npm run type-check
         - run: cd frontend && npm run lint
         - run: cd frontend && npm test
   ```

3. **CD — Build & Push Docker images**:
   ```yaml
   name: Build & Push
   on:
     push:
       tags: ['v*']
   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: docker/login-action@v3
           with:
             registry: ghcr.io
             username: ${{ github.actor }}
             password: ${{ secrets.GITHUB_TOKEN }}
         - uses: docker/build-push-action@v5
           with:
             context: ./backend
             push: true
             tags: ghcr.io/omni-saude/austa-backend:${{ github.ref_name }}
   ```

4. **CD — Deploy to Kubernetes** (workflow separado, trigger manual):
   ```yaml
   name: Deploy to Staging
   on: workflow_dispatch
   jobs:
     deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: aws-actions/configure-aws-credentials@v4
           with:
             role-to-assume: arn:aws:iam::...
             aws-region: sa-east-1
         - run: aws eks update-kubeconfig --name austa-staging
         - run: kubectl apply -f k8s/namespace.yaml
         - run: kubectl apply -f k8s/deployments/ -f k8s/services/
         - run: kubectl rollout status deployment/austa-backend -n austa-staging
   ```

5. Adicionar `.github/dependabot.yml` para atualização automática de dependências

**Verification:**
- Push → backend CI roda e passa (type-check, lint, test)
- Push → frontend CI roda e passa
- Tag `v*` → Docker image builda e pusha para ghcr.io

---

### Agente 3-C: Complete Infrastructure (K8s, Monitoring, Terraform)

**Especialista:** `ops-containers-k8s` + `ops-observability`
**Toolsets:** terminal, file, search, skills

**Context:** K8s manifests são bons mas incompletos (sem NetworkPolicy, PVC, ServiceMonitor). Prometheus alert rules existem mas Grafana dashboards não são as-code. Terraform foi completado na Wave 0.

**Goal:** Completar os manifestos Kubernetes, criar dashboards Grafana como código, e garantir que a stack de observabilidade está completa.

**Tasks:**
1. **Kubernetes — completar manifests**:
   - Criar `k8s/configmaps/backend-config.yaml` (variáveis não-secretas)
   - Criar `k8s/network-policy.yaml` (restringir tráfego entre pods)
   - Criar `k8s/pvc/` (PersistentVolumeClaims se necessários)
   - Criar `k8s/service-monitor.yaml` (para Prometheus Operator)
   - Atualizar `k8s/hpa.yaml` com métricas customizadas se relevante

2. **Grafana Dashboards as Code**:
   - Criar dashboard "AUSTA — API Overview": latência P50/P95/P99, taxa de erro, throughput
   - Criar dashboard "AUSTA — Clinical Metrics": scores de risco/dia, alertas de emergência/dia, tempo médio de resposta WhatsApp
   - Criar dashboard "AUSTA — Business Metrics": onboardings completados, NPS, HealthPoints distribuídos
   - Todos em `monitoring/grafana/dashboards/` como JSON

3. **Prometheus Alert Rules** — revisar 17 regras existentes:
   - Adicionar alerta: `HighApiErrorRate` (>1% erros em 5min)
   - Adicionar alerta: `EncryptionHealthCheckFailed`
   - Adicionar alerta: `AuditLogPersistenceFailed`
   - Adicionar alerta: `DeadMansSwitchSilent` (sem health check por >5min)

4. **Criar `scripts/deploy-staging.sh`**:
   ```bash
   #!/bin/bash
   set -e
   kubectl apply -f k8s/namespace.yaml
   kubectl apply -f k8s/configmaps/
   kubectl apply -f k8s/secrets/  # (secrets gerenciados externamente)
   kubectl apply -f k8s/deployments/
   kubectl apply -f k8s/services/
   kubectl apply -f k8s/ingress.yaml
   kubectl apply -f k8s/hpa.yaml
   kubectl apply -f k8s/network-policy.yaml
   echo "Deploy complete. Checking status..."
   kubectl rollout status deployment/austa-backend -n austa-staging --timeout=5m
   ```

**Verification:**
- `kubectl apply --dry-run=client -f k8s/` passa em todos os manifests
- 3 novos dashboards Grafana exportados como JSON
- Script de deploy funcional (dry-run)

---

## 6. Wave 4 — DOCUMENTATION & ADRs

**Objetivo:** Atualizar documentação de arquitetura para refletir a realidade, escrever ADRs formais, gerar OpenAPI spec, e preparar artefatos finais.

**Dependência:** Waves 0-3 completas.

**2-3 agentes paralelos.**

### Agente 4-A: Update Architecture Documentation + Write ADRs

**Especialista:** Leaf agent (Technical Writing)
**Toolsets:** terminal, file, search

**Goal:** Atualizar a documentação de arquitetura para refletir o que REALMENTE existe (TypeScript monolith, Z-API, PostgreSQL-only). Escrever os 6 ADRs propostos em formato formal.

**Tasks:**
1. **Atualizar `docs/architecture_diagrams.md`**:
   - Remover diagrama C4Container que mostra Python/Java (substituir por diagrama honesto: 1 backend TS + PostgreSQL + Redis)
   - Atualizar C4Context: Z-API em vez de "WhatsApp Business API (Meta)"
   - Adicionar nota: "A arquitetura atual é um modular monolith. Microservices serão extraídos sob demanda (ver ADR-003)."
   - Manter diagramas de Security, Monitoring, Deployment (ainda aspiracionais mas úteis)

2. **Escrever ADRs formais** em `docs/architecture/adr/`:
   - `ADR-001-resolve-hippa-lgpd-confusion.md` — decisão de substituir HIPAA por LGPD
   - `ADR-002-anvisa-samd-classification.md` — encaminhamento para classificação ANVISA
   - `ADR-003-monolith-first-architecture.md` — decisão de manter monolith para MVP
   - `ADR-004-encryption-strategy-phI.md` — estratégia de envelope encryption com pgcrypto
   - `ADR-005-clinical-algorithm-versioning.md` — versionamento de algoritmos clínicos
   - `ADR-006-idempotency-message-processing.md` — idempotência para mensagens WhatsApp/FHIR/Tasy
   
   Formato padrão: Title, Status (Accepted/Proposed), Context, Decision, Alternatives Considered, Consequences, Trade-offs

3. **Atualizar `README.md`** do repositório com honestidade sobre estado atual

**Verification:**
- 6 ADRs em `docs/architecture/adr/ADR-0XX-*.md`
- architecture_diagrams.md reflete stack real (TypeScript, não Python/Java)

---

### Agente 4-B: Generate OpenAPI Spec + Operations Runbook

**Especialista:** Leaf agent (Documentation/API)
**Toolsets:** terminal, file, search, skills

**Goal:** Gerar especificação OpenAPI 3.0 das rotas Express, escrever runbook de operações, e preparar documentação de onboarding para desenvolvedores.

**Tasks:**
1. **OpenAPI/Swagger Spec**:
   - Criar `docs/api/openapi.yaml` com todas as rotas atuais
   - Rotas mínimas:
     - `GET /api/v1/health` — Health check
     - `POST /api/v1/auth/login` — Login
     - `POST /api/v1/auth/register` — Registro
     - `POST /api/v1/auth/refresh` — Refresh token
     - `GET /api/v1/users/:id` — Perfil do usuário
     - `POST /api/v1/webhooks/whatsapp` — Webhook WhatsApp
     - `POST /api/v1/authorizations` — Criar autorização
     - `GET /api/v1/authorizations/:id` — Status de autorização
   - Incluir schemas de request/response, autenticação, erros
   - Adicionar `docs/api/README.md` com instruções para visualizar (Swagger UI)

2. **Operations Runbook** (`docs/OPERATIONS-RUNBOOK.md`):
   - Como fazer deploy (staging e produção)
   - Como verificar health do sistema
   - Como responder a alertas comuns (HighApiErrorRate, DeadMansSwitchSilent)
   - Procedimento de rollback
   - Procedimento de backup/restore do PostgreSQL
   - Procedimento de rotação de secrets
   - Contatos de emergência (placeholders)

3. **Developer Onboarding** (`docs/DEVELOPER-GUIDE.md`):
   - Pré-requisitos: Node.js 18+, Docker, PostgreSQL 15
   - Setup: `docker compose up -d postgres redis && npm install && npx prisma generate && npx prisma db push && npm run dev`
   - Estrutura do projeto
   - Como rodar testes
   - Convenções de código

**Verification:**
- OpenAPI spec valida em https://editor.swagger.io (estrutura correta)
- Runbook cobre todos os cenários de falha comuns

---

### Agente 4-C: Final Compliance Review + Update Artifacts

**Especialista:** `ops-compliance-gate`
**Toolsets:** terminal, file, search, skills

**Goal:** Revisão final de compliance pós-migração. Atualizar HANDOFF.yaml, review-queue.md. Verificar se todos os achados críticos do PLATFORM-REVIEW.md foram endereçados.

**Tasks:**
1. **Revisão de compliance pós-migração**:
   - Verificar: 0 referências a HIPAA no código
   - Verificar: pgcrypto funcional (teste de encrypt/decrypt)
   - Verificar: AuditLog persistente (registro sobrevive restart)
   - Verificar: Algoritmos registram versão
   - Verificar: Mensagens WhatsApp têm idempotência
   - Verificar: Secrets não estão hardcoded

2. **Atualizar `HANDOFF.yaml`**:
   - Mover itens concluídos para `completed:`
   - Adicionar data de conclusão
   - Manter bloqueios pendentes em `blocked:`

3. **Atualizar `docs/review-queue.md`**:
   - Marcar itens concluídos como APPROVED (se validados) ou manter DRAFT
   - Adicionar data de revisão

4. **Criar `PROJECT_STATUS.md`** (raiz do repo):
   - Status honesto do projeto
   - O que funciona, o que está em progresso, o que está bloqueado
   - Link para PLATFORM-REVIEW.md
   - Badge de build status (se CI configurado)

5. **Verificação final**: rodar `npm test && npm run lint && npm run type-check` e reportar resultado

**Verification:**
- HANDOFF.yaml tem `completed:` com itens das Waves 0-4
- PROJECT_STATUS.md existe e é honesto
- Testes passam (ou documentar falhas)

---

## 7. DEMO — Spawn Tree Demonstration

**Objetivo:** Demonstrar o Spawn Tree UI com um agente visível.

**Tarefa:** Despachar um agente simples que executa `sleep 15` e depois imprime "Spawn Tree demo complete! ✅". O usuário pode abrir o Spawn Tree durante esses 15 segundos e ver o agente em execução.

```
delegate_task(
  goal: "Wait 15 seconds and print a completion message to demonstrate the Spawn Tree UI",
  context: "Use terminal: sleep 15 && echo 'Spawn Tree demo complete! ✅'",
  toolsets: ["terminal"]
)
```

---

## 8. Verificação Final (pós Wave 4)

### 8.1 Verificações Automatizadas

```bash
# 1. TypeScript compila?
cd backend && npx tsc --noEmit
cd frontend && npx tsc --noEmit

# 2. Testes passam?
cd backend && npm test -- --passWithNoTests
cd frontend && npm test -- --passWithNoTests

# 3. Lint passa?
cd backend && npm run lint
cd frontend && npm run lint

# 4. Zero HIPAA references?
grep -r "HIPAA\|hipaa" --include="*.ts" --include="*.md" --include="*.json" --include="*.sql" --include="*.prisma" . | grep -v node_modules | grep -v ".git" | grep -v "_analysis"

# 5. Secrets hardcoded?
grep -r "password.*:.*[a-z].*[0-9]" docker-compose*.yml

# 6. Terraform válido?
cd austa-care-platform/infrastructure/terraform && terraform fmt -check -recursive

# 7. Docker build funciona?
cd backend && docker build -t austa-backend:test .
cd frontend && docker build -t austa-frontend:test .

# 8. Health check endpoint?
curl -s http://localhost:3000/api/v1/health | jq .
```

### 8.2 Artefatos Finais Esperados

| Artefato | Status Esperado |
|----------|-----------------|
| `backend/src/server.ts` | Limpo, compila, zero erros TS |
| `backend/src/app.ts` | Express app factory bem estruturado |
| `backend/src/lib/crypto.ts` | pgcrypto envelope encryption |
| `backend/src/lib/retry.ts` | Retry com exponential backoff |
| `backend/src/lib/algorithm-registry.ts` | Versões de algoritmos |
| `backend/tests/` | Infra unificada, sem duplicação |
| `docs/architecture/adr/ADR-00*.md` | 6 ADRs formais |
| `docs/api/openapi.yaml` | OpenAPI 3.0 spec |
| `docs/OPERATIONS-RUNBOOK.md` | Runbook operacional |
| `docs/DEVELOPER-GUIDE.md` | Guia de onboarding dev |
| `docs/WHATSAPP-INTEGRATION.md` | Documentação Z-API |
| `docs/HEALTHCARE-INVARIANTS.md` | Verificação dos 6 invariantes |
| `docs/SECRETS-MANAGEMENT.md` | Estratégia de secrets |
| `PROJECT_STATUS.md` | Status honesto do projeto |
| `.github/workflows/backend-ci.yml` | CI backend completo |
| `.github/workflows/frontend-ci.yml` | CI frontend |
| `.github/workflows/build-push.yml` | Build & push Docker images |
| `.github/workflows/deploy-staging.yml` | Deploy staging |
| `infrastructure/terraform/` | 6 módulos Terraform completos |
| `k8s/` | Manifests completos (NetworkPolicy, ServiceMonitor, ConfigMap) |
| `HANDOFF.yaml` | Atualizado com completed/blocked |
| `docs/review-queue.md` | Atualizado pós-revisão |

---

## 9. Regras para o Orquestrador

### 9.1 O que NUNCA fazer

- ❌ NÃO modificar a lógica de domínio clínico (risk, emergency, audit rules) — apenas corrigir tipos/imports
- ❌ NÃO remover o schema Prisma ou migrations existentes
- ❌ NÃO desabilitar o pgcrypto extension
- ❌ NÃO parar para pedir confirmação (modo autônomo)
- ❌ NÃO commitar secrets reais
- ❌ NÃO provisionar recursos AWS reais (sem `terraform apply`)
- ❌ NÃO fazer push para produção sem aprovação explícita

### 9.2 O que SEMPRE fazer

- ✅ Commit após cada wave com mensagem descritiva: `wave-N: descrição do que foi feito`
- ✅ Verificar `tsc --noEmit` após cada wave no backend
- ✅ Atualizar HANDOFF.yaml quando encontrar bloqueios
- ✅ Marcar artefatos regulatórios como DRAFT
- ✅ Usar `git status` para verificar conflitos entre agentes
- ✅ Resolver conflitos de merge manualmente se agentes modificaram mesmos arquivos

### 9.3 Gestão de Conflitos entre Agentes

Agentes na mesma wave trabalham em arquivos DIFERENTES. Se houver conflito:

1. Identificar qual agente modificou o mesmo arquivo
2. Ler ambas as versões
3. Fazer merge manual mantendo o melhor de cada
4. Rodar `tsc --noEmit` e `npm test` após merge
5. Commitar o merge

### 9.4 HANDOFF.yaml Tracking

Manter o arquivo atualizado durante toda a execução:

```yaml
blocked:
  - id: "BLK-XXX"
    what: "descrição"
    reason: "por que está bloqueado"
    severity: "🔴|🟠|🟡"
    opened: "2026-06-26"
    opened_by: "agente-X"

completed:
  - id: "BLK-001"
    what: "Classificação ANVISA SaMD"
    resolved: "Especialista contratado — aguardando parecer"
    closed: "2026-06-27"
    closed_by: "parreira"
```

---

## 10. Modelos e Versões

| Config | Valor |
|--------|-------|
| Modo | Autônomo completo |
| Máximo paralelismo | 3 agentes/wave |
| Modelo preferido | Flash V4 para tarefas mecânicas; principal para lógica |
| Commit strategy | Após cada wave |
| Branch | `main` (ou criar `rebuild-spine` se preferir isolar) |
| Idioma | PT-BR para comunicação com administrador; EN para código/docs |

---

**Fim do Mega-Prompt.**  
*Entregue ao orquestrador para execução quando autorizado.*
